pipeline {
	agent any
	stages {
		stage('master') {
			steps {
				sh 'echo "this is master"'
			}
		}
		stage('development') {
			steps {
				sh 'echo "this is development"'
			}
		}
		stage('production') {
			steps {
				sh 'echo "this is production"'
			}
		}
	}
}
